---
label: Billet de blog
hide_body: false
display_field: title
fields:
- name: date
  type: datetime
  label: Date
  description: ''
  config:
    required: true
    date_format: DD-MM-YYYY
    time_format: 
    display_utc: false
    export_format: YYYY-MM-DD
  default: now
- type: text
  name: title
  label: Titre
- type: text
  name: slug
  label: Slug (titre pour l'URL)
  config:
    min: 1
  description: Titre en minuscules, sans accent, apostrophe, cédille, ni espace. Par
    exemple "c-est-comme-ca-qu-il-faut-faire".
- name: poster
  type: file
  config:
    maxSize: 10
  label: Poster
